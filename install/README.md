# Database Initialization Files

This directory contains the initial database schema and example data
used during installation.

- Tables are created automatically during setup.
- Example blocks are provided to prevent a blank homepage.
- These files do not contain any sensitive or user-specific data.

You may safely remove this directory after installation is complete.